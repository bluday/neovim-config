-- Default config extracted from `LspInfo`

return {
    settings = {
        Lua = {
            codeLens = {
                enable = true
            },
            hint = {
                enable = true,
                semicolon = "Disable"
            }
        }
    }
}
